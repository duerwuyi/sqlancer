name: ci

on:
  push:
    branches:
      - main
      - citus_test
    paths:
      - 'src/**'
      - 'test/**'
      - '.github/workflows/*.yml'
      - 'pom.xml'
  pull_request:
    branches:
      - main
      - citus_test
    paths:
      - 'src/**'
      - 'test/**'
      - '.github/workflows/*.yml'
      - 'pom.xml'

jobs:
  citus:
    name: DBMS Tests (Citus)
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        slice: [1, 2, 3, 4]   # 4 个 slice ≈ 24h 总预算
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'
          cache: 'maven'
      - name: Build SQLancer
        run: mvn -B package -DskipTests=true
      - name: Set up Citus
        run: |
          # echo "deb http://apt.postgresql.org/pub/repos/apt/ `lsb_release -cs`-pgdg main" | sudo tee  /etc/apt/sources.list.d/pgdg.list
          sudo apt-get update
          curl https://install.citusdata.com/community/deb.sh | sudo bash
          sudo apt-get -y install  postgresql-17-citus-13.0
          sudo chown -R $USER:$USER /var/run/postgresql
          export PATH=/usr/lib/postgresql/17/bin:$PATH
          cd ~
          mkdir -p citus/coordinator citus/worker1 citus/worker2
          initdb -D citus/coordinator
          initdb -D citus/worker1
          initdb -D citus/worker2
          echo "shared_preload_libraries = 'citus'" >> citus/coordinator/postgresql.conf
          echo "shared_preload_libraries = 'citus'" >> citus/worker1/postgresql.conf
          echo "shared_preload_libraries = 'citus'" >> citus/worker2/postgresql.conf
          pg_ctl -D citus/coordinator -o "-p 9700" -l coordinator_logfile start || cat coordinator_logfile || cat citus/coordinator/coordinator_logfile
          pg_ctl -D citus/worker1 -o "-p 9701" -l worker1_logfile start
          ls citus/worker1
          pg_ctl -D citus/worker2 -o "-p 9702" -l worker2_logfile start
          psql -c "CREATE ROLE sqlancer SUPERUSER LOGIN CREATEDB PASSWORD 'sqlancer';" -p 9700 -d postgres -U $USER
          createdb test -p 9700 -U $USER
          psql -c "CREATE ROLE sqlancer SUPERUSER LOGIN CREATEDB PASSWORD 'sqlancer';" -p 9701 -d postgres -U $USER
          createdb test -p 9701 -U $USER
          psql -c "CREATE ROLE sqlancer SUPERUSER LOGIN CREATEDB PASSWORD 'sqlancer';" -p 9702 -d postgres -U $USER
          createdb test -p 9702 -U $USER
          psql -c "CREATE EXTENSION citus;" -p 9700 -U $USER -d test
          psql -c "CREATE EXTENSION citus;" -p 9701 -U $USER -d test
          psql -c "CREATE EXTENSION citus;" -p 9702 -U $USER -d test
          psql -c "SELECT * from citus_add_node('localhost', 9701);" -p 9700 -U $USER -d test
          psql -c "SELECT * from citus_add_node('localhost', 9702);" -p 9700 -U $USER -d test
      - name: Run Tests
        run: CITUS_AVAILABLE=true mvn -Dtest=TestCitus test
      - name: Upload logs as artifact
        uses: actions/upload-artifact@v4
        with:
          name: citus-logs-slice-${{ matrix.slice }}
          path: |
            target/logs/
            sqlancer_citus_slice${{ matrix.slice }}.out
  postgres:
    name: DBMS Tests (PostgreSQL)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Set up PostgreSQL
        uses: harmon758/postgresql-action@v1.0.0
        with:
          postgresql version: '12'
          postgresql user: 'sqlancer'
          postgresql password: 'sqlancer'
          postgresql db: 'test'
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'
          cache: 'maven'
      - name: Build SQLancer
        run: mvn -B package -DskipTests=true
      - name: Run Tests
        run: |
          POSTGRES_AVAILABLE=true mvn -Dtest=TestPostgresPQS test
          POSTGRES_AVAILABLE=true mvn -Dtest=TestPostgresTLP test
          POSTGRES_AVAILABLE=true mvn -Dtest=TestPostgresNoREC test
          POSTGRES_AVAILABLE=true mvn -Dtest=TestPostgresCERT test
  # citus2:
  #   name: DBMS Tests (Citus)2
  #   runs-on: ubuntu-22.04
  #   timeout-minutes: 355
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       slice: [1, 2, 3, 4]   # 4 个 slice ≈ 24h 总预算
  #   steps:
  #     - uses: actions/checkout@v3
  #       with:
  #         fetch-depth: 0

  #     - name: Set up JDK 11
  #       uses: actions/setup-java@v3
  #       with:
  #         distribution: 'temurin'
  #         java-version: '11'
  #         cache: 'maven'

  #     - name: Build SQLancer
  #       run: mvn -B package -DskipTests=true

  #     - name: Set up Citus
  #       run: |
  #         sudo apt-get update
  #         curl https://install.citusdata.com/community/deb.sh | sudo bash
  #         sudo apt-get -y install postgresql-17-citus-13.0
  #         sudo chown -R $USER:$USER /var/run/postgresql
  #         export PATH=/usr/lib/postgresql/17/bin:$PATH
  #         cd ~
  #         mkdir -p citus/coordinator citus/worker1 citus/worker2
  #         initdb -D citus/coordinator
  #         initdb -D citus/worker1
  #         initdb -D citus/worker2
  #         echo "shared_preload_libraries = 'citus'" >> citus/coordinator/postgresql.conf
  #         echo "shared_preload_libraries = 'citus'" >> citus/worker1/postgresql.conf
  #         echo "shared_preload_libraries = 'citus'" >> citus/worker2/postgresql.conf
  #         pg_ctl -D citus/coordinator -o "-p 9700" -l coordinator_logfile start
  #         pg_ctl -D citus/worker1 -o "-p 9701" -l worker1_logfile start
  #         pg_ctl -D citus/worker2 -o "-p 9702" -l worker2_logfile start
  #         psql -c "CREATE ROLE sqlancer SUPERUSER LOGIN CREATEDB PASSWORD 'sqlancer';" -p 9700 -d postgres -U $USER
  #         createdb test -p 9700 -U $USER
  #         psql -c "CREATE ROLE sqlancer SUPERUSER LOGIN CREATEDB PASSWORD 'sqlancer';" -p 9701 -d postgres -U $USER
  #         createdb test -p 9701 -U $USER
  #         psql -c "CREATE ROLE sqlancer SUPERUSER LOGIN CREATEDB PASSWORD 'sqlancer';" -p 9702 -d postgres -U $USER
  #         createdb test -p 9702 -U $USER
  #         psql -c "CREATE EXTENSION citus;" -p 9700 -U $USER -d test
  #         psql -c "CREATE EXTENSION citus;" -p 9701 -U $USER -d test
  #         psql -c "CREATE EXTENSION citus;" -p 9702 -U $USER -d test
  #         psql -c "SELECT * from citus_add_node('localhost', 9701);" -p 9700 -U $USER -d test
  #         psql -c "SELECT * from citus_add_node('localhost', 9702);" -p 9700 -U $USER -d test

  #     - name: Run SQLancer (slice ~5h50m) and report bugs immediately
  #       env:
  #         SLICE_SECONDS: "21000"       # 5h50m，给 job 收尾留余量
  #         MAX_BUGS: "1000000"          # 找到很多个 bug 才退出
  #       run: |
  #         set -euo pipefail

  #         JAR="$(ls -1 target/sqlancer-*.jar | head -n 1)"
  #         echo "Using JAR: $JAR"

  #         mkdir -p target/logs
  #         SEEN_LIST="$RUNNER_TEMP/seen_bugs.txt"
  #         : > "$SEEN_LIST"

  #         # 直接跑 SQLancer（不要走 JUnit），输出保存到文件，同时在 Actions log 里可见
  #         java -jar "$JAR" \
  #           --timeout-seconds "$SLICE_SECONDS" \
  #           --num-tries "$MAX_BUGS" \
  #           citus \
  #           --host localhost --port 9700 --username sqlancer --password sqlancer --database test \
  #           2>&1 | tee "sqlancer_citus_slice${{ matrix.slice }}.out" &
  #         PID=$!

  #         # 轮询：一旦发现新的 bug 复现 .log（非 *-cur.log），立刻打点并输出末尾内容
  #         while kill -0 "$PID" 2>/dev/null; do
  #           for f in target/logs/*.log; do
  #             [ -e "$f" ] || continue
  #             case "$f" in
  #               *-cur.log) continue ;;
  #             esac
  #             if ! grep -Fxq "$f" "$SEEN_LIST"; then
  #               echo "$f" >> "$SEEN_LIST"
  #               echo "[$(date -u +%FT%TZ)] FOUND BUG LOG: $f"
  #               echo "----- tail(120) of $f -----"
  #               tail -n 120 "$f" || true
  #               echo "--------------------------------"
  #             fi
  #           done
  #           sleep 10
  #         done

  #         # 不因为退出码让 job 失败；你要的是“跑起来并汇总结果”
  #         wait "$PID" || true

  #         echo "Slice finished at $(date -u +%FT%TZ)"
  #         echo "Bug logs:"
  #         ls -lah target/logs || true

  #         {
  #           echo "### Citus slice ${{ matrix.slice }} summary"
  #           echo "- Finished: $(date -u +%FT%TZ)"
  #           echo "- Bug logs (reproducers):"
  #           find target/logs -maxdepth 1 -type f -name "*.log" ! -name "*-cur.log" -print || true
  #           echo ""
  #           echo "Last progress lines:"
  #           tail -n 30 "sqlancer_citus_slice${{ matrix.slice }}.out" || true
  #         } >> "$GITHUB_STEP_SUMMARY"

  #     - name: Upload logs as artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: citus-logs-slice-${{ matrix.slice }}
  #         path: |
  #           target/logs/
  #           sqlancer_citus_slice${{ matrix.slice }}.out
